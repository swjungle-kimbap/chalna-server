spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chalna
    username: chalna
    password: 1q2w3e4r
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      password: