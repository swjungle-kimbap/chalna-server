version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/chalna-bucket-deploy
permissions:
  - object: /home/ubuntu/chalna-bucket-deploy
    owner: ubuntu
    group: ubuntu
    mode: 755